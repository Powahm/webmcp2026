# Vercel CLI does not reliably fall back to .gitignore, and raw/ is ~15GB of
# Companies House bulk downloads. Only the source the build needs is uploaded;
# public/corpus/*.json is the built corpus and MUST ship.

node_modules
raw
dist
.git
.env
.env.local
*.tsbuildinfo
*.log
*.tgz
