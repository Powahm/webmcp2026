# Companies House REST API key.
# Register for a free key at:
#   https://developer.company-information.service.gov.uk/
# Create an application, then generate a REST API key (not a streaming key).
#
# Used ONLY by scripts/ during offline corpus ingestion.
# It is never read by src/ and never appears in the client bundle.
CH_API_KEY=
